NRSD(1)               User Contributed Perl Documentation              NRSD(1)

NAME
     nrsd - Net::Radius::Server-based RADIUS server

SYNOPSIS
         nrsd [options]

DESCRIPTION
     This script uses Net::Radius::Server::NS(3), which uses Net::Server(3),
     to start a RADIUS server using the Net::Radius::Server(3) framework.

     See Net::Radius::Server::NS(3) for information on the required
     configuration parameters. Typically, this script can be invoked as in:

       nrsd --conf_file nrsd.cfg

HISTORY
       $Log$
       Revision 1.1  2006/11/07 22:51:07  lem
       Initial revision

LICENSE AND WARRANTY
     This code and all accompanying software comes with NO WARRANTY. You use
     it at your own risk.

     This code and all accompanying software can be used freely under the same
     terms as Perl version 5.8.6 itself.

AUTHOR
     Luis E. Muñoz <luismunoz@cpan.org>

SEE ALSO
     perl(1), Net::Radius::Server(3), Net::Radius::Server::NS(3).

POD ERRORS
     Hey! The above document had some coding errors, which are explained
     below:

     Around line 50:
         Non-ASCII character seen before =encoding in 'Muñoz'. Assuming UTF-8

perl v5.42.3                      2009-09-20                           NRSD(1)
