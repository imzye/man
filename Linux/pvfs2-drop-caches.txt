PVFS2-DROP-CACHES(1)        General Commands Manual       PVFS2-DROP-CACHES(1)

NAME
       pvfs2-drop-caches – flush I/O cache on OrangeFS servers

SYNOPSIS
       pvfs2-drop-caches [-m fs_mount_point]

DESCRIPTION
       The  pvfs2-drop-caches utility requests that each OrangeFS server flush
       the operating system I/O cache.

       The options are as follows:

       -m     Specify the file system to operate on.

ENVIRONMENT
       PVFS2_DEBUGFILE
              If set to the path of a local file, redirect debug output to it.

       PVFS2_DEBUGMASK
              Set the OrangeFS debug mask.  Possible masks are  documented  in
              pvfs2-set-debugmask(1).

       PVFS2TAB_FILE
              If set, the full pathname for an alternate pvfs2tab file

FILES
       /etc/pvfs2tab

BUGS
       Please submit bug reports to pvfs2-developers@beowulf-underground.org

SEE ALSO
       pvfs2tab(5)

                                  2017-07-03              PVFS2-DROP-CACHES(1)
