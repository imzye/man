resalloc-maint(1)                                                                           General Commands Manual                                                                           resalloc-maint(1)

NAME
       resalloc-maint

SYNOPSIS
       resalloc-maint [-h] {resource-list,resource-delete,ticket-list,foreach-resource} ...

OPTIONS
   Sub-commands
       resalloc-maint resource-list
              List available resources

       resalloc-maint resource-delete
              Delete resource

       resalloc-maint ticket-list
              List not-yet-closed tickets

       resalloc-maint foreach-resource
              Execute command for each resource

OPTIONS 'resalloc-maint resource-list'
       usage: resalloc-maint resource-list [-h] [--up]

       --up   List only ready-to-take resources

OPTIONS 'resalloc-maint resource-delete'
       usage: resalloc-maint resource-delete [-h] resource [resource ...]

       resource
              The resource IDs

OPTIONS 'resalloc-maint ticket-list'
       usage: resalloc-maint ticket-list [-h]

OPTIONS 'resalloc-maint foreach-resource'
       usage: resalloc-maint foreach-resource [-h] [--command COMMAND]

       --command COMMAND
              Shell command to execute. This accepts variables like {state}, {data_utf8},
              {name}, etc.

AUTHORS
       resalloc was written by Pavel Raiskup <praiskup@redhat.com>.

DISTRIBUTION
       The latest version of resalloc may be downloaded from ⟨https://github.com/praiskup/resalloc⟩

                                                                                                     Manual                                                                                   resalloc-maint(1)
