CSV2BIN(1)                       User Commands                      CSV2BIN(1)

NAME
       csv2bin - IBM Hardware Accelerator Tool.

DESCRIPTION
       csv2bin -i <Input CSV File> -o <Output Bin File>

       -crc Add crc32 to bin file (same as from chksum).

       -v Verbose mode.

       -crc Add crc32 to bin file (same as from chksum).

       -v Verbose mode.

csv2bin -i <Input CSV File> -o <OutJulyB2021ile>                    CSV2BIN(1)
