COMPOSER-CLI(1)                                                                                                                                                                                 COMPOSER-CLI(1)

NAME
       composer-cli - composer commandline tool

SYNOPSIS
       composer-cli [flags]

DESCRIPTION
       commandline tool for osbuild-composer

OPTIONS
       -a, --api=1      Server API Version to use

       -h, --help[=false]      help for composer-cli

       -j, --json[=false]      Output the raw JSON response instead of the normal output

       --log=""      Path to optional logfile

       -s, --socket="/run/weldr/api.socket"      Path to the server's socket file

       --test=0      Pass test mode to compose. 1=Mock compose with fail. 2=Mock compose with finished.

       --timeout=240      Timeout to use for server communication. Set to 0 for no timeout

SEE ALSO
       composer-cli-blueprints(1),   composer-cli-completion(1),   composer-cli-compose(1),  composer-cli-distros(1),  composer-cli-doc(1),  composer-cli-modules(1),  composer-cli-projects(1),  composer-cli-
       sources(1), composer-cli-status(1), composer-cli-version(1)

HISTORY
       29-Oct-2021 Auto generated by spf13/cobra

composer-cli                                                                                        Oct 2021                                                                                    COMPOSER-CLI(1)
