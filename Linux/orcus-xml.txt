USAGE:(1)                                                                                        User Commands                                                                                        USAGE:(1)

NAME
       Usage: - convert an XML file

SYNOPSIS
       orcus-xml [OPTIONS] FILE

OPTIONS
       -h [ --help ]
              Print this help.

       --mode arg
              Mode of operation. Select one of the following options: dump, map, map-gen, structure, or transform.

       -m [ --map ] arg
              Path to the map file. A map file is required for all modes except for the structure mode.

       -o [ --output ] arg
              Path to either an output directory, or an output file.

       -f [ --output-format ] arg Specify the output format.
              Supported format types are:

       * check - Flat format that fully encodes document
              content.  Suitable for automated testing.  * csv - CSV format.  * flat - Flat text format that displays document content in grid.  * html - HTML format.  * json - JSON format.  * none - No out‐
              put to be generated. Maybe useful during development.  * xml - This format is currently unsupported.

       -h [ --help ]
              Print this help.

       --mode arg
              Mode of operation. Select one of the following options: dump, map, map-gen, structure, or transform.

       -m [ --map ] arg
              Path to the map file. A map file is required for all modes except for the structure mode.

       -o [ --output ] arg
              Path to either an output directory, or an output file.

       -f [ --output-format ] arg Specify the output format.
              Supported format types are:

       * check - Flat format that fully encodes document
              content. Suitable for automated testing.  * csv - CSV format.  * flat - Flat text format that displays document content in grid.  * html - HTML format.  * json - JSON format.  * none - No  out‐
              put to be generated. Maybe useful during development.  * xml - This format is currently unsupported.

liborcus 0.16.1                                                                                   August 2021                                                                                         USAGE:(1)
