DJANGO_EXTENSIONS(1)                                                                             User Commands                                                                             DJANGO_EXTENSIONS(1)

NAME
       django_extensions is not installed; I do not use - autoradiod daemon for autoradio suite

SYNOPSIS
       autoradiod <action> [options] (action=start|stop|restart|run|version)

DESCRIPTION
       django_extensions is not installed; I do not use it

       autoradiod daemon for autoradio suite

OPTIONS
       --version
              show program's version number and exit

       -h, --help
              show this help message and exit

       --pidfile=PIDFILE
              PID filename (default /tmp/autoradiod.lock)

       --stdin=STDIN
              stdin filename (default /dev/null)

       --stdout=STDOUT
              stdout filename (default /tmp/autoradiod.log)

       --stderr=STDERR
              stderr filename (default /tmp/autoradiod.err)

       --user=USER
              user name or id (default none)

       --group=GROUP
              group name or id (default none)

       autoradiod 3.4

django_extensions it                                                                               March 2020                                                                              DJANGO_EXTENSIONS(1)
