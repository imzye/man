VPOLLER-WORKER(1)                                                                                User Commands                                                                                VPOLLER-WORKER(1)

NAME
       vpoller-worker - manual page for vpoller-worker 0.7.3

SYNOPSIS
       vpoller-worker [-d] [-c <concurrency>] [-f <config>] start

DESCRIPTION
              vpoller-worker [-d] -e <endpoint> stop vpoller-worker [-d] -e <endpoint> status vpoller-worker --help vpoller-worker --version

   Arguments:
       start  Start the VPoller Worker

       stop   Stop the VPoller Worker

       status Get status information

OPTIONS
       -h, --help
              Display this usage info

       -v, --version
              Display version and exit

       -d, --debug
              Debug mode, be more verbose

       -c <concurrency>, --concurrency <concurrency>
              The number of worker processes to be created.  The default is the number of CPUs available on the system.

       -f <config>, --file <config>
              Specify config file to use [default: /etc/vpoller/vpoller.conf]

       -e <endpoint>, --endpoint <endpoint>
              Specify the endpoint we connect to

SEE ALSO
       The full documentation for vpoller-worker is maintained as a Texinfo manual.  If the info and vpoller-worker programs are properly installed at your site, the command

              info vpoller-worker

       should give you access to the complete manual.

vpoller-worker 0.7.3                                                                               July 2021                                                                                  VPOLLER-WORKER(1)
