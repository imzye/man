compute_tangent_hypergraph(1)                                                                    User Commands                                                                    compute_tangent_hypergraph(1)

NAME
       compute_tangent_hypergraph - compute tangent hypergraph in a tropical cone

DESCRIPTION
       Computes the tangent directed hypergraph at a given point in a tropical cone.

ARGUMENTS
       -numerical-data
              Set the type of numerical data used by the algorithm, one of: ocaml_int, ocaml_float, ocaml_big_int, ocaml_big_rat, zarith_int, and zarith_rat.

       -generators
              Specify the cone from a generating family.

       -half-spaces
              Specify the cone from half-spaces.

       -min-plus
              Use the min-plus semiring instead of the default max-plus semiring.

       -help, --help
              Display this list of options.

SEE ALSO
       compute_ext_rays(1), compute_ext_rays_polar(1), compute_halfspaces(1), compute_minimal_external_representations(1), compute_tropical_complex(1)

TPLib 1.3                                                                                           Feb 2013                                                                      compute_tangent_hypergraph(1)
