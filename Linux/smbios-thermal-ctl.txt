SMBIOS-THERMAL-CTL(1)                                                                            User Commands                                                                            SMBIOS-THERMAL-CTL(1)

NAME
       smbios-thermal-ctl - ../src/bin/smbios-thermal-ctl

SYNOPSIS
       smbios-thermal-ctl

OPTIONS
       --version
              show program's version number and exit

       -h, --help
              show this help message and exit

       -i, --info
              This will Display the Supported Features of USTT and AAC

       -g, --get-thermal-info
              This will Display the Thermal Information of a system

       --set-thermal-mode=THERMAL_MODE
              Option to set Thermal Mode; balanced, cool-bottom, quiet, performance

       -v, --verbose
              Display more verbose output.

       -q, --quiet
              Minimize program output. Only errors and warnings are displayed.

       --trace
              Enable verbose function tracing.

       --logconfig=LOGCONFIG
              Specify alternate config log.

       --security-key=SECURITY_KEY
              BIOS pre-calculated security key.

       --password=PASSWORD
              BIOS Setup password for set/activate operations.

       -r, --rawpassword
              Do not auto-convert password to scancodes.

       --memory-dat=MEMORY_DAT
              Path to a memory dump file to use instead of real RAM

       --cmos-dat=CMOS_DAT
              Path to a CMOS dump file to use instead of real CMOS

SEE ALSO
       The full documentation for smbios-thermal-ctl is maintained as a Texinfo manual.  If the info and smbios-thermal-ctl programs are properly installed at your site, the command

              info smbios-thermal-ctl

       should give you access to the complete manual.

smbios-thermal-ctl 2.4.3                                                                           July 2021                                                                              SMBIOS-THERMAL-CTL(1)
