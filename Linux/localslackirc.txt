lsi-getconf(1)                                                     General Commands Manual                                                     lsi-getconf(1)

NAME
       lsi-getconf - Read the browser database to try to find the cookie and token that are necessary to set up localslackirc.

SYNOPSIS
       lsi-getconf [OPTIONS]

DESCRIPTION
       This command reads the browser database to try to find token and cookie that are necessary to localslackirc.
       For now it only supports Firefox.
       This is experimental. It might not work on your machine. In which case you should fall back to extracting the data directly from the browser.

   Options:
       -h, --help
              Show the help message and exit.

       --mozilla-dir DIRECTORY
              Alternative search path for the mozilla directory. It defaults to ~/.mozilla.

Known errors
       database is locked
              Close the browser to solve this issue.

SEE ALSO
       localslackirc(1)

WEB
       https://codeberg.org/ltworf/localslackirc

AUTHOR
       Salvo "LtWorf" Tomaselli <tiposchi@tiscali.it>

Send files to slack                                                      Jun 18, 2024                                                          lsi-getconf(1)
