viagee(1)                                                                                    User Commands                                                                                   viagee(1)

NAME
       viagee - Integrate GMail into the GNOME desktop

SYNOPSIS
       viagee [-r file|<mailto-URL>] [--send] [-u <user-email>|--user <user-email>]

DESCRIPTION
       The  viagee  script  opens a GMail page on the default web browser, with a message composed according to the supplied RFC2368 mailto: URL. This script is used to support GMail as a GNOME Pre‐
       ferred Application for email.

OPTIONS
       A mailto: URL. The 'To', 'CC', 'BCC', 'Subject', and 'Body' fields are supported. The 'Attach' keyword is used to identify files to be attached to the message. If the  argument  is  not  well
       formed, it is assumed to be the recipient. If no argument is supplied, a default GMail page is opened.

       -s, --send
              send immediately

       -u, --user
              specify the 'from' address, overriding the stored choice

       -r file, --rfc file
              send the RFC822-formatted file file

FILES
       ~/.config/viagee/viagee.conf - Configuration file

BUGS
       No known bugs.

AUTHOR
       David Steele (dsteele@gmail.com)

Linux                                                                                         7 June 2015                                                                                    viagee(1)
