WARNING:(1)                                                                                       --version""                                                                                       WARNING:(1)

NAME
       Warning: - manual page for Warning: unrecognized option "--version"

DESCRIPTION
       Warning: unrecognized option "--help" NVIDIA Texture Tools 2.1.2 - Copyright NVIDIA Corporation 2007

       usage: nvcompress [options] infile [outfile.dds]

   Input options:
       -color The input image is a color map (default).

       -alpha The input image has an alpha channel used for transparency.

       -normal
              The input image is a normal map.

       -tonormal
              Convert input to normal map.

       -clamp Clamp wrapping mode (default).

       -repeat
              Repeat wrapping mode.

       -nomips
              Disable mipmap generation.

       -premula
              Premultiply alpha into color channel.

       -mipfilter
              Mipmap filter. One of the following: box, triangle, kaiser.

       -float Load as floating point image.

       -rgbm  Transform input to RGBM.

       -rangescale
              Scale image to use entire color range.

   Compression options:
       -fast  Fast compression.

       -nocuda
              Do not use cuda compressor.

       -rgb   RGBA format

       -lumi  LUMINANCE format

       -bc1   BC1 format (DXT1)

       -bc1n  BC1 normal map format (DXT1nm)

       -bc1a  BC1 format with binary alpha (DXT1a)

       -bc2   BC2 format (DXT3)

       -bc3   BC3 format (DXT5)

       -bc3n  BC3 normal map format (DXT5nm)

       -bc4   BC4 format (ATI1)

       -bc5   BC5 format (3Dc/ATI2)

       -bc6   BC6 format

       -bc7   BC7 format

       -bc3_rgbm
              BC3-rgbm format

   Output options:
       -silent
              Do not output progress messages

       -dds10 Use DirectX 10 DDS format (enabled by default for BC6/7, unless ktx is being used)

       -ktx   Use KTX container format

       -srgb  If the requested format allows it, output will be in sRGB color space

       NVIDIA Texture Tools 2.1.2 - Copyright NVIDIA Corporation 2007

       usage: nvcompress [options] infile [outfile.dds]

   Input options:
       -color The input image is a color map (default).

       -alpha The input image has an alpha channel used for transparency.

       -normal
              The input image is a normal map.

       -tonormal
              Convert input to normal map.

       -clamp Clamp wrapping mode (default).

       -repeat
              Repeat wrapping mode.

       -nomips
              Disable mipmap generation.

       -premula
              Premultiply alpha into color channel.

       -mipfilter
              Mipmap filter. One of the following: box, triangle, kaiser.

       -float Load as floating point image.

       -rgbm  Transform input to RGBM.

       -rangescale
              Scale image to use entire color range.

   Compression options:
       -fast  Fast compression.

       -nocuda
              Do not use cuda compressor.

       -rgb   RGBA format

       -lumi  LUMINANCE format

       -bc1   BC1 format (DXT1)

       -bc1n  BC1 normal map format (DXT1nm)

       -bc1a  BC1 format with binary alpha (DXT1a)

       -bc2   BC2 format (DXT3)

       -bc3   BC3 format (DXT5)

       -bc3n  BC3 normal map format (DXT5nm)

       -bc4   BC4 format (ATI1)

       -bc5   BC5 format (3Dc/ATI2)

       -bc6   BC6 format

       -bc7   BC7 format

       -bc3_rgbm
              BC3-rgbm format

   Output options:
       -silent
              Do not output progress messages

       -dds10 Use DirectX 10 DDS format (enabled by default for BC6/7, unless ktx is being used)

       -ktx   Use KTX container format

       -srgb  If the requested format allows it, output will be in sRGB color space

Warning: unrecognized option                                                                       July 2021                                                                                        WARNING:(1)
